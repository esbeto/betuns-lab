#!/bin/bash

# Usage
# Run script, then `yarn` to install modules
# Then `yarn start`

sudo mount -t tmpfs -o rw,size=50M tmpfs $PWD/dist
sudo mount -t tmpfs -o rw,size=1G tmpfs $PWD/node_modules

# yarn
